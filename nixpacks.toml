[phases.setup]
nixPkgs = ["python313Packages.pip", "python313"]

[phases.install]
cmds = ["pip install -r backend/requirements.txt"]

[start]
cmd = "uvicorn api.server:app --host 0.0.0.0 --port $PORT --app-dir backend"
