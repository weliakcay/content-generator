[phases.setup]
nixPkgs = ["python313", "gcc"]

[phases.install]
cmds = ["cd backend && pip install -r requirements.txt"]

[start]
cmd = "cd backend && uvicorn api.server:app --host 0.0.0.0 --port $PORT"
